import { Text } from 'react-native';
import { styles } from '../Style/Styles';

export const Logo = () => {
    return (
        <Text style={styles.logo}>Words Generator</Text>
        )
}

export default Logo;